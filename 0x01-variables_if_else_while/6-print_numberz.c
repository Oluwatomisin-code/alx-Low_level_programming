#include <stdio.h>

/**
 *  * main - Entry point
 *   * diplays alphabet a -z
 *    * Return: 0
 *     */
int main(void)
{
	int a;
	
	for (a = 0; a < 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
