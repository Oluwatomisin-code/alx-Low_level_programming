#include <stdio.h>
#include <main.h>

/**
 * main - Entry point
 * _putchar function include in main.h writes to std
 * Return: 0 
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{		
		_putchar(c);
	}
	_putchar('\n');
	return (0);
}
