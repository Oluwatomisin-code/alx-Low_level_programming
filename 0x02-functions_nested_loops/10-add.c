#include "main.h"

/**
 * add - adds to numbers a and b together
 * @a: first parameter
 * @b: second parameter
 * Return: returns sum of numbers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
